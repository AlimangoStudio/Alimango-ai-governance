# Claude Code Reference Adapter

Use root `CLAUDE.md` as the host shim and root `AGENTS.md` as the shared control entry. Load the repository Constitution and applicable policies before behavior-changing work.

Host-specific skills and hooks may strengthen validation but cannot grant capability, bypass approval, reorder authority, or override public-scope constraints.
