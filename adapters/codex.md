# Codex Reference Adapter

Codex should treat root `AGENTS.md` as the entry point. Repository tools are capabilities, not automatic permissions. Use the narrowest sandbox/tool set compatible with the task, route material writes through Spec Kit/action governance, and report exact evidence/terminal state.

Codex-host configuration may strengthen isolation or approvals but may not replace the repository's portable control contracts.
