# UI Quality Policy

For contributions that change a user-facing interface, functional correctness is not sufficient.

Check established design patterns, responsive behavior, keyboard/focus behavior, readable contrast, overflow/clipping, error/empty/loading states, and touch targets where relevant. Prefer shared components/tokens over one-off visual systems. UI evidence should match the breakpoints/themes actually affected.