# Adversarial Review

Assume the implementation may be wrong in a non-obvious way. Search for bypasses, fail-open paths, scope expansion, stale assumptions, unsafe defaults, hidden side effects, prompt/tool injection, secret leakage, supply-chain trust mistakes, incomplete negative tests, and claims stronger than evidence.

Attack the control, not the contributor. Findings must be specific and reproducible where practical.