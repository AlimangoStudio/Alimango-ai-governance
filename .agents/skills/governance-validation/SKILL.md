# Governance Validation

Validate the governance system as code.

Check required control files, machine-readable manifests, authority order, public/private boundaries, schema syntax, workflow/skill registry consistency, broken references, fail-closed invariants, and whether a governance change weakens or deletes the very validator needed to review it.

Governance changes should receive the same or stronger evidence discipline as application changes.