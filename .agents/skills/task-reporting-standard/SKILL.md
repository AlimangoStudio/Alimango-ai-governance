# Task Reporting Standard

Final reports distinguish: requested, implemented, tested, reviewed, merged, released, deployed, and live-verified.

Include changed surfaces, relevant revisions, validation executed, failed/skipped checks, independent findings, open gates, security/capability changes, and rollback/deployment state when applicable.

Never compress an open gate into “done with minor follow-up.” Use the correct terminal state.