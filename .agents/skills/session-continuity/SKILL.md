# Session Continuity

Do not rely on conversational memory as the only project state.

For interrupted or multi-agent work, persist: objective, authority revision, current spec, branch/commit, changed files, commands/checks run, evidence, open findings, blocked capabilities, and the next valid action. A new session must re-verify mutable facts before acting.