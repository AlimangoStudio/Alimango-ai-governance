# Agent Governance Benchmarks

Benchmark governance mechanisms, not marketing claims.

A useful benchmark measures both **control effectiveness** and **cost**. Results should identify model/provider/runtime, governance revision, fixture set, run count, randomness settings when available, and environment limitations.

Suggested metric definitions are in `METRICS.md`. Public benchmark results are evidence for research, not production certification.